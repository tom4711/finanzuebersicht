<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Finanzuebersicht.Controls.DashboardInsightRow"
             x:Name="Root">

    <Border StrokeShape="RoundRectangle 8"
            Stroke="Transparent"
            BackgroundColor="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}"
            Padding="12,10">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding Source={x:Reference Root}, Path=TapCommand}" />
        </Border.GestureRecognizers>
        <Grid ColumnDefinitions="*, Auto" ColumnSpacing="8">
            <VerticalStackLayout Spacing="2" VerticalOptions="Center">
                <Label Text="{Binding Source={x:Reference Root}, Path=Title}"
                       FontSize="14"
                       FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                <Label Text="{Binding Source={x:Reference Root}, Path=Detail}"
                       FontSize="13"
                       LineBreakMode="TailTruncation"
                       TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}"
                       IsVisible="{Binding Source={x:Reference Root}, Path=HasDetail}" />
            </VerticalStackLayout>
            <Label Grid.Column="1"
                   Text="›"
                   FontSize="20"
                   FontAttributes="Bold"
                   VerticalTextAlignment="Center"
                   AutomationProperties.IsInAccessibleTree="False"
                   TextColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource Gray600}}" />
        </Grid>
    </Border>
</ContentView>
